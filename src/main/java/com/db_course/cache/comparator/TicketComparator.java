package com.db_course.cache.comparator;

import com.db_course.dto.TicketDto;

import java.util.Comparator;

public class TicketComparator implements Comparator<TicketDto> {
    @Override
    public int compare(TicketDto o1, TicketDto o2) {
        return 0;
    }

}
